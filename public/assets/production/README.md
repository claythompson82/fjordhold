# Production Assets

This tree holds runtime-ready assets used by Phaser.

Asset loading is controlled by `asset_pack_v1_index.json`.

- Add files under the appropriate category folders.
- Register key/path entries in the index.
- Set `enabled: true` only after validation.
