# Concept Mockups

Place reference-only concept sheets in this tree.

These files guide style and production slicing, but should not be treated as final runtime sprites.

Use lowercase snake_case names.
