# Concept Art References

Store large concept boards and reference mockups here (non-runtime docs space).

- `raw/` keeps source concept sheets, brainstorm boards, and unsliced references.
- `exports/` keeps curated/processed exports used as handoff references for production asset creation.

These files are reference-only and are **not** loaded by the game at runtime.
Use lowercase snake_case names.
